if not timerexists "taste"
    createtimer "taste"
    settimer "taste" 16000
endif

if timer 'taste' >= 10500
    useskill 'TasteId'
    waitfortarget
    targetrelloc 0 0
    settimer 'taste' 0
endif

loop