clearignore 
while findtype "recall rune" as runa
    getlabel runa desc
    if unknown in desc
        dclick runa
        waitforprompt any
        promptresponse "SYN GH"
        wait 50
    else
        overhead desc
    endif
    ignore runa
endwhile